import cv2

for i in range(1,6):
    vidcap = cv2.VideoCapture(str(i)+".gif")
    success,image = vidcap.read()
    count = 0
    success = True
    while success:
        cv2.imwrite(str(i) + str(count) + ".jpg", image)
        success,image = vidcap.read()
        count += 1
